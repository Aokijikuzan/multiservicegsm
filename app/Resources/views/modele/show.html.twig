
{% extends 'base.html.twig' %}

{% block body %}
     {% include '::menuAction.html.twig' %} 
    <h1 style="text-align:center;">Modele</h1>
    <div class="container">
        <div class="row">
            <table class="table table-striped table-bordered" style="border: 15px solid grey; border-spacing:10px;">
                <tbody>
                    <tr scope="row"  style=" border:1px solid grey; text-align:center;">
                 <!--       <th scope="row"  style=" border:1px solid grey;"></th>
                 -->    <th style="text-align:center;">Id</th>
                        <td style="text-align:center;">{{ modele.id }}</td>
                    </tr>
                    <tr scope="row"  style=" border:1px solid grey; ">
                        <th style="text-align:center;">Nom du modele</th>
                        <td style="text-align:center;">{{ modele.nomModele }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
       
            <ul scope="row"  style=" border:1px solid grey; text-align:center;">
                <li>
                    <a href="{{ path('modele_index') }}">Revenir en arriere</a>
                </li>
                <li>
                    <a href="{{ path('modele_edit', { 'id': modele.id }) }}">Modifier </a>
                </li>
                <li style="text-align:center;">
                    <a href="{{ path('modele_new') }}" >ajouter un nouveau modèle</a>
                </li>
                <li>
                    {{ form_start(delete_form) }}
                        <input type="submit" value="Supprimer">
                    {{ form_end(delete_form) }}
                </li>
            </ul>
        
    </div>
{% endblock %}
