{% extends 'base.html.twig' %}

{% block body %} 
   {% include '::menuAction.html.twig' %} 
    <h1>liste des modeles </h1>

    <table class="table table-striped table-bordered" style="border: 15px solid grey; border-spacing:10px;">
        <thead>
            <tr style="text-align:center;">
                <th scope="row"  style=" border:1px solid grey;"></th>
                <th style="text-align:auto;">Id</th>
                <th style="text-align:center;">Nommodele</th>
                <th style="text-align:center;">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for modele in modeles %}
            <tr  style="text-align:center;">
                <th scope="row" style=" border:1px solid grey;"></th>
                <td><a href="{{ path('modele_show', { 'id': modele.id }) }}">{{ modele.id }}</a></td>
                <td>{{ modele.nomModele }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('modele_show', { 'id': modele.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('modele_edit', { 'id': modele.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('modele_new') }}">Create a new modele</a>
        </li>
    </ul>
    {% include '::footer.html.twig' %}
{% endblock %}

