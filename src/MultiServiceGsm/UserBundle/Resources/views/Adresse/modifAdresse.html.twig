{% extends 'base.html.twig' %}

{% block body %}
    {% include '::menu.html.twig' %} 
        <p class="col-md-2" ><a href="{{path('fos_user_profile_show')}}" title="Lien 1">Profil</a></p>
    <div class="container">
        <div class="row" style="margin-left:250px; margin-right:auto;">

            <div class="col-lg-12 col-lg-offset 6">
                <div class="well well-lg col-lg-6">
                    <h1 style="text-align:center;">Modifier une adresse</h1>
                        <div style="text-align:center;" > 
                        	{{ form_start(modi_form) }}
                        		{{ form_widget(modi_form) }}
                        		<input type="submit" name="Modifier" />
                        	{{ form_end(modi_form) }}
                        </div>
                        <br>
                         <ul style="text-align:center;">
	                        <li>
	                            <a href="{{ path('multi_service_gsm_user_show_adresse') }}">Revenir sur la liste d'adresse</a>
	                        </li>
	    
	                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
